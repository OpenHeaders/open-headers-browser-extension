# Disabled — development moved to https://github.com/OpenHeaders/open-headers-app
name: CI (disabled)
on: workflow_dispatch
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This repo is archived. CI runs in OpenHeaders/open-headers-app."
