# Disabled — releases are cross-published from https://github.com/OpenHeaders/open-headers-app
name: Build and Release (disabled)
on: workflow_dispatch
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This repo is archived. Releases are published from OpenHeaders/open-headers-app."
